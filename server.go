package main

import(
	"fmt"
)

// connect to localhost
func initServer() {

}

func join(input string) {
	fmt.Println(input + " has joined the chat!")
}

func broadcast(){
//RPC call
//needs to be logged
}

func publish(){
//RPC call
//needs to be logged
}


func leave(){
// disconnects from localhost - input a username?
}